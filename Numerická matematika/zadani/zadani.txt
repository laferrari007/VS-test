# Zadání úkolů pro předmět: Numerická matematika
# Vygenerováno: AI
# Datum: 2025-09-15 23:17:48

Úkol 1: Implementujte numerickou metodu pro aproximaci kruhového čísla π. Použijte metodu Monte Carlo, kde generujete náhodná čísla v intervalu (0,1) a určujete poměr čísel, která leží v daném kruhu. Zkuste implementovat alespoň 10 iterací a porovnejte výsledky s aproximací π.

Úkol 2: Použijte numerické metody pro aproximaci funkce f(x) = 1/x. Implementujte metodu Newtonova-Raphsona s počátečním předpokladem x0 = 1 a určete číslo iterací, které vede ke stabilitě a přesnosti výsledku. Diskutujte vliv počtu iterací na přesnost výsledku.

Úkol 3: Implementujte numerickou metodu pro řešení systému lineárních rovnic Ax = b. Použijte metodu Gaussa-Seidela pro řešení systému s vícerozložitými maticemi A a b. Porovnejte výsledky s řešením pomocí metody Choleského.

Úkol 4: Použijte numerické metody pro aproximaci integrálu funkce f(x) = 1/x^2. Implementujte metodu Simpsonovy, kde budete použít čtvrtinné intervaly a určujete počet iterací. Porovnejte výsledky s aproximací pomocí metody Trapezoidu.

Úkol 5: Implementujte numerickou metodu pro řešení diferenciálních rovnic y' = -y, kde y(0) = 1. Použijte metodu Eulerovy pro řešení diferenciální rovnice s počátečním předpokladem y0 = 1 a určit počet iterací, které vede ke stabilitě a přesnosti výsledku.

Úkol 6: Použijte numerické metody pro aproximaci funkce f(x) = sin(x). Implementujte metodu Lagrangeovy, která využívá interpolaci Lagrangeovy sítě. Porovnejte výsledky s aproximací pomocí metody Newtonovy.

Úkol 7: Implementujte numerickou metodu pro řešení systému lineárních rovnic Ax = b, kde A je maticí s vícerozložitými prvky. Použijte metodu QR, která spočívá v rozkladu maticy A na kvadratinici Q a horní trojúhelníkovou matici R. Porovnejte výsledky s řešením pomocí metody Gaussa-Seidela.

Úkol 8: Použijte numerické metody pro aproximaci integrálu funkce f(x) = e^(-x^2). Implementujte metodu Gaussa-Kronrodova, která spočívá v aproximaci integrálu pomocí Gaussova kvadraturu a následném korekci pomocí metody Kronrodovy. Porovnejte výsledky s aproximací pomocí metody Simpsonovy.

Úkol 9: Implementujte numerickou metodu pro řešení diferenciálních rovnic y'' + 2y' + y = 0, kde y(0) = 0 a y'(0) = 1. Použijte metodu Runge-Kutta, která spočívá v použití čtyřstupeňové integrace a posunu. Porovnejte výsledky s řešením pomocí metody Eulerovy.

Úkol 10: Použijte numerické metody pro aproximaci funkce f(x) = log(x). Implementujte metodu Newtonova-Raphsona s počátečním předpokladem x0 = 1 a určete číslo iterací, které vede ke stabilitě a přesnosti výsledku. Diskutujte vliv počtu iterací na přesnost výsledku a porovnejte výsledky s aproximací pomocí metody Lagrangeovy.
