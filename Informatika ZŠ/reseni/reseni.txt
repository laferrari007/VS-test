# AI Generované řešení úkolů pro předmět: Informatika ZŠ
# Vytvořeno pomocí Groq API
# Datum: 2025-09-15 21:23:09
# POZOR: Řešení odpovídají konkrétním úkolům ze zadání!

Řešení 1: Zobrazí vaše jméno a roční stupeň na obrazovce.

```python
# Vstupní data
jmeno = "Jan Novák"
rok_stupna = 5

# Vypis na obrazovku
print("Můj jméno je:", jmeno)
print("Jsem ve stupni:", rok_stupna)
```

Krok 1: Definujeme vstupní data `jmeno` a `rok_stupna`.

Krok 2: Používáme funkci `print()` pro výpis na obrazovku.

Řešení 2: Vypíše všechny čísla od 1 do 100, která jsou dělitelná jak 3, tak 5.

```python
# Vypis čísel, která jsou dělitelná jak 3, tak 5
for i in range(1, 101):
    if i % 3 == 0 and i % 5 == 0:
        print(i)
```

Krok 1: Používáme cyklus `for` pro iteraci přes čísla od 1 do 100.

Krok 2: Používáme podmínky `i % 3 == 0` a `i % 5 == 0` pro vyhledání čísel, která jsou dělitelná jak 3, tak 5.

Krok 3: Vypisujeme čísla, která splňují podmínky.

Řešení 3: Spočítá a vypíše součet všech čísel od 1 do 100.

```python
# Spočítá součet čísel od 1 do 100
soucet = 0
for i in range(1, 101):
    soucet += i

# Vypis součtu
print("Součet čísel od 1 do 100 je:", soucet)
```

Krok 1: Definujeme proměnnou `soucet` a inicializujeme ji na 0.

Krok 2: Používáme cyklus `for` pro iteraci přes čísla od 1 do 100.

Krok 3: Používáme operátor `+=` pro přidání každého čísla k proměnné `soucet`.

Krok 4: Vypisujeme součet po ukončení cyklu.

Řešení 4: Vypíše všechny prvočísla od 1 do 20.

```python
# Vypis prvočísel od 1 do 20
for i in range(1, 21):
    primitel = True
    for j in range(2, int(i ** 0.5) + 1):
        if i % j == 0:
            primitel = False
            break
    if primitel:
        print(i)
```

Krok 1: Používáme cyklus `for` pro iteraci přes čísla od 1 do 20.

Krok 2: Používáme vnitřní cyklus `for` pro vyhledání dělitelnosti čísla `i` na další čísla.

Krok 3: Používáme podmínku `primitel` pro ověření, zda číslo je prvočíslo.

Krok 4: Vypisujeme prvočísla, která splňují podmínku.

Řešení 5: Ptá uživatele na své jméno a poté ho přivítá a požádá o zadání věku. Pokud je uživatel mladší 18 let, vypíše program informaci, že uživatel je dítě.

```python
# Ptá uživatele na své jméno a věk
jmeno = input("Zadejte své jméno: ")
vek = int(input("Zadejte svůj věk: "))

# Přivítání a vyhodnocení věku
print("Vítejte,", jmeno + "!")
if vek < 18:
    print("Budu vás považovat za dítě.")
else:
    print("Budu vás považovat za dospělého.")
```

Krok 1: Používáme funkci `input()` pro získání vstupních dat od uživatele.

Krok 2: Používáme podmínku `vek < 18` pro vyhodnocení věku uživatele.

Krok 3: Vypisujeme informaci o věku uživatele.

Řešení 6: Vypíše všechny měsíce roku, v nichž je 31 dní.

```python
# Vypis měsíců s 31 dny
měsíce = ["leden", "březen", "duben", "květen", "červen", "srpen", "září", "listopad", "prosinec"]
for měsíc in měsíce:
    if měsíc in ["leden", "duben", "květen", "srpen", "listopad", "prosinec"]:
        print(měsíc)
```

Krok 1: Používáme seznam `měsíce` pro uchovávání měsíců roku.

Krok 2: Používáme cyklus `for` pro iteraci přes měsíce.

Krok 3: Používáme podmínku `měsíc in ["leden", "duben", "květen", "srpen", "listopad", "prosinec"]` pro vyhledání měsíců s 31 dny.

Krok 4: Vypisujeme měsíce s 31 dny.

Řešení 7: Spočítá a vypíše průměrnou hodnotu čísel od 1 do 100.

```python
# Spočítá průměrnou hodnotu čísel od 1 do 100
soucet = 0
for i in range(1, 101):
    soucet += i
prumer = soucet / 100

# Vypis průměrné hodnoty
print("Průměrná hodnota čísel od 1 do 100 je:", prumer)
```

Krok 1: Definujeme proměnnou `soucet` a inicializujeme ji na 0.

Krok 2: Používáme cyklus `for` pro iteraci přes čísla od 1 do 100.

Krok 3: Používáme operátor `+=` pro přidání každého čísla k proměnné `soucet`.

Krok 4: Spočítáváme průměrnou hodnotu pomocí operátoru `/`.

Krok 5: Vypisujeme průměrnou hodnotu.

Řešení 8: Vypíše všechny čísla od 1 do 100, která jsou větší než 50.

```python
# Vypis čísel větších než 50
for i
